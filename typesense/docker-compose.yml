services:
  typesense:
    image: typesense/typesense:30.2
    ports:
      - '8108:8108'
    volumes:
      - ./typesense-data:/data
    command: '--data-dir /data --api-key=xyz --enable-cors'
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
